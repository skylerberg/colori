[package]
name = "colori-runner"
version = "0.1.0"
edition = "2021"

[features]
default = []
nn-ai = ["colori-core/nn-ai"]

[dependencies]
colori-core = { path = "../colori-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
rand = "0.10"
wyrand = "0.4"
