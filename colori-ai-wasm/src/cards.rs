// Card definitions are not needed as static data since cards come from JSON game state.
// The card data (names, abilities, pips) is embedded in each CardInstance's `card` field.
// The PRIMARIES, SECONDARIES, TERTIARIES constants are in colors.rs.
